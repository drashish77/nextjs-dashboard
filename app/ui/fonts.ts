import { Inter, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-1' });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  variable: '--font-2',
  subsets: ['latin'],
});
